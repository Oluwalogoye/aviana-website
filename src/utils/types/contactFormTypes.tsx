export interface ContactFormType {
  title: string;
  firstName: boolean;
  lastName: boolean;
  emailAddress: boolean;
  phoneNumber: boolean;
}

export interface FormInformation {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  message: string;
}